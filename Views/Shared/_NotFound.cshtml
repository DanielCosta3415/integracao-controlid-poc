@{
    ViewData["Title"] = "Não Encontrado";
}

<div class="container mt-5">
    <h2 class="text-warning">Página não encontrada (404)</h2>
    <p>A página solicitada não existe ou foi removida.</p>
    <a href="@Url.Action("Index", "Home")" class="btn btn-primary">Voltar para o início</a>
</div>