@model Integracao.ControlID.PoC.ViewModels.AccessRules.AccessRuleEditViewModel
@{
    ViewData["Title"] = "Editar regra de acesso";
    var header = new AppPageHeaderViewModel
    {
        Eyebrow = "Pessoas",
        Title = "Editar regra de acesso",
        Description = "Atualize uma regra oficial preservando clareza de prioridade e legibilidade dos parâmetros operacionais.",
        BadgeText = "Edição",
        BadgeTone = "warning",
        Actions = new[]
        {
            new PageHeaderActionViewModel { Label = "Voltar para regras", Href = Url.Action("Index")!, ButtonClass = "btn btn-outline-secondary" }
        }
    };
}

<div class="container mt-5 app-stack">
    <partial name="_AppPageHeader" model="header" />

    <section class="detail-card app-stack">
        <div class="entity-overview">
            <div class="entity-overview__item"><span>ID</span><strong>@(Model.Id?.ToString() ?? "Novo")</strong></div>
            <div class="entity-overview__item"><span>Nome</span><strong>@(string.IsNullOrWhiteSpace(Model.Name) ? "Não definido" : Model.Name)</strong></div>
            <div class="entity-overview__item"><span>Prioridade</span><strong>@Model.Priority</strong></div>
        </div>
    </section>

    <section class="app-surface-card app-stack">
        <div class="app-page-section__heading">
            <div>
                <strong>Editar parâmetros</strong>
                <p>Revise nome, tipo e prioridade antes de confirmar as alterações desta regra de acesso.</p>
            </div>
        </div>

        <form asp-action="Edit" method="post" autocomplete="off" class="form-section">
            <input type="hidden" asp-for="Id" />
            <div class="app-form-grid">
                <div>
                    <label asp-for="Name" class="form-label"></label>
                    <input asp-for="Name" class="form-control" />
                    <span asp-validation-for="Name" class="text-danger"></span>
                </div>
                <div>
                    <label asp-for="Type" class="form-label"></label>
                    <input asp-for="Type" class="form-control" />
                    <span asp-validation-for="Type" class="text-danger"></span>
                </div>
                <div>
                    <label asp-for="Priority" class="form-label"></label>
                    <input asp-for="Priority" class="form-control" />
                    <span asp-validation-for="Priority" class="text-danger"></span>
                </div>
            </div>

            <div class="form-actions">
                <button type="submit" class="btn btn-primary">Salvar alterações</button>
                <a asp-action="Index" class="btn btn-outline-secondary">Cancelar</a>
            </div>
        </form>
    </section>
</div>

@section Scripts {
    <partial name="_ValidationScriptsPartial" />
}
